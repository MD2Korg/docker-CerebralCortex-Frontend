<%= form_for(@datasource) do |f| %>
  <% if @datasource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@datasource.errors.count, "error") %> prohibited this datasource from being saved:</h2>

      <ul>
      <% @datasource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :identifier %><br>
    <%= f.text_field :identifier %>
  </div>
  <div class="field">
    <%= f.label :datasourcetype %><br>
    <%= f.text_field :datasourcetype %>
  </div>
  <div class="field">
    <%= f.label :datadescriptor %><br>
    <%= f.text_field :datadescriptor %>
  </div>
  <div class="field">
    <%= f.label :metadata %><br>
    <%= f.text_field :metadata %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
