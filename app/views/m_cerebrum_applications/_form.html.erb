<%= form_for(@m_cerebrum_application) do |f| %>
  <% if @m_cerebrum_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@m_cerebrum_application.errors.count, "error") %> prohibited this m_cerebrum_application from being saved:</h2>

      <ul>
      <% @m_cerebrum_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :apptype %><br>
    <%= f.text_field :apptype %>
  </div>
  <div class="field">
    <%= f.label :identifier %><br>
    <%= f.text_field :identifier %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
